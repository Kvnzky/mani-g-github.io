// Google Sheets configuration is managed securely server-side via environment variables.
// Sensitive IDs and Web App URLs are never exposed in public client bundles.
export const DEFAULT_SPREADSHEET_ID = '';
export const DEFAULT_APPS_SCRIPT_URL = '';
